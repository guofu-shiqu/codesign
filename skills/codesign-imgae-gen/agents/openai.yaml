interface:
  display_name: "CoDesign Imgae Gen"
  short_description: "Generate an image into a selected CoDesign AI holder"
  default_prompt: "Use $codesign:codesign-imgae-gen to generate an image into the selected CoDesign AI image holder."

policy:
  allow_implicit_invocation: true
